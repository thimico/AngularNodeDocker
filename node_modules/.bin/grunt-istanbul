#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-istanbul/bin/grunt-istanbul" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-istanbul/bin/grunt-istanbul" "$@"
  ret=$?
fi
exit $ret
