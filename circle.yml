machine:
  node:
    version: 0.11.8
    
dependencies:
  pre:
    - npm install
    - npm install handlebars 
  #  - npm install -g gulp-cli
  
 ## post:
   # - node server.js
  
test:
  override:
    - mocha
    
    
